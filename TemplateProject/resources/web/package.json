{
  "name": "templateproject-web-ui",
  "version": "1.0.0",
  "description": "TemplateProject Web UI",
  "scripts": {
    "build": "tsc && esbuild scripts/index.js --bundle --format=iife --global-name=TemplateProjectUI --outfile=scripts/index.bundle.js",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.12",
    "typescript": "^5.3.0"
  }
}
